package com.bootcamp.demo;

// stateless -> No Attribute
public class PaymentService {
    // PaymentService has no state.
    // private int state; (Assume it doesn't slow/break down.)

    public boolean pay() {
        return true;
    }
}
